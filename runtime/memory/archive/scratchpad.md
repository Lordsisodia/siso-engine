# Scratchpad

Volatile notes while iterating on the Blackbox system.

Promote durable content into:

- `domains/` (domain knowledge)
- `.skills/` (repeatable methods)
- `.prompts/` (reusable instruction packs)
- `.agents/` (agent package updates)

