# Journal (append-only)

This is a **system journal** for the Blackbox itself (not for a specific run).

Use it to log:

- structural changes to `docs/.blackbox/`
- new conventions (IDs, run naming, status rules)
- why a prompt/skill/agent was updated

