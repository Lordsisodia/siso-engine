version: 1
box:
  id: siso-internal-docs
  name: "SISO Internal Blackbox (Docs)"
  description: "Domain-based agent runtime: runs + agents + prompts + skills + domains."
  owner: "SISO"

paths:
  protocol: "protocol.md"
  context: "context.md"
  tasks: "tasks.md"
  journal: "journal.md"
  scratchpad: "scratchpad.md"

  domains_dir: "domains"
  agents_dir: ".agents"
  skills_dir: ".skills"
  prompts_dir: ".prompts"
  runs_dir: ".runs"

  docs_ledger: "../process/docs-ledger.md"
  docs_routing: "../process/information-routing.md"

