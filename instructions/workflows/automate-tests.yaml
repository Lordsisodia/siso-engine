# Workflow: Automate Tests
# Automate Tests

name: automate-tests
command: QA
description: Automate Tests
skill: bmad-qa
agent: Quinn
complexity: medium
steps:
- name: start
  title: Start
  description: Start Automate Tests
  actions:
  - Initialize workflow
  - Create WIP file
- name: execute
  title: Execute
  description: Execute Automate Tests
  actions:
  - Perform main workflow
  - Track progress
- name: complete
  title: Complete
  description: Complete Automate Tests
  actions:
  - Finalize
  - Update WIP
  - Archive
apc_menu:
  enabled: true
  options:
    advanced:
      label: Advanced
      description: Full workflow with all steps, detailed documentation
    party:
      label: Party
      description: Multi-agent collaborative mode
    continue:
      label: Continue
      description: Resume from last saved checkpoint
wip_tracking:
  enabled: true
  directory: ./wip
  filename_pattern: qa-{name}-{timestamp}.md
outputs:
- type: artifact
  location: ./artifacts/automate-tests-{name}.md
  format: markdown
- type: wip
  location: ./wip/{command}-{name}-{timestamp}.md
  format: markdown
verification:
- WIP file created and updated
- All steps completed
- Output artifacts generated
- Tests cover requirements
- Test quality verified
- Coverage adequate
- Tests pass
