# Workflow: Test Review
# Test Review

name: test-review
command: TR
description: Test Review
skill: bmad-tea
agent: TEA (Test Architect)
complexity: medium
steps:
- name: start
  title: Start
  description: Start Test Review
  actions:
  - Initialize workflow
  - Create WIP file
- name: execute
  title: Execute
  description: Execute Test Review
  actions:
  - Perform main workflow
  - Track progress
- name: complete
  title: Complete
  description: Complete Test Review
  actions:
  - Finalize
  - Update WIP
  - Archive
apc_menu:
  enabled: true
  options:
    advanced:
      label: Advanced
      description: Full workflow with all steps, detailed documentation
    party:
      label: Party
      description: Multi-agent collaborative mode
    continue:
      label: Continue
      description: Resume from last saved checkpoint
wip_tracking:
  enabled: true
  directory: ./wip
  filename_pattern: tr-{name}-{timestamp}.md
outputs:
- type: artifact
  location: ./artifacts/test-review-{name}.md
  format: markdown
- type: wip
  location: ./wip/{command}-{name}-{timestamp}.md
  format: markdown
verification:
- WIP file created and updated
- All steps completed
- Output artifacts generated
