# GitHub Search Queries — Search Topics (Facets, Autocomplete, Adapters)

Use this query bank to bias OSS discovery toward:
- storefront search UX primitives (autocomplete, facets, refinements)
- admin search/filter UX (saved views, query builders, filter chips)
- adapter layers (InstantSearch ↔ Typesense/Meilisearch, etc.)

Recommended run mode:
- `--no-derived-queries` (stay focused; avoid feature-map bridge query pollution)
- `--min-stars 200` for high-signal, lower only if you need niche adapters

## Autocomplete + facets UI primitives (components)

- `autocomplete ui react open source`
- `react autosuggest component open source`
- `faceted search ui react open source`
- `filters sidebar react open source`
- `filter chips component react open source`
- `query builder react open source`
- `search command palette react open source`

## InstantSearch + adapters (integration glue)

- `instantsearch adapter open source`
- `typesense instantsearch adapter`
- `meilisearch instantsearch adapter`
- `algolia autocomplete open source`
- `search suggestions api open source`

## Lightweight search backends + clients (prototype-friendly)

- `typesense javascript client`
- `meilisearch javascript client`
- `vector search javascript client open source`
- `sqlite full text search javascript open source`

## Topics to browse (fast)

- `typesense`, `meilisearch`, `instantsearch`, `autocomplete`, `search-ui`
- `cmdk`, `combobox`, `downshift`, `headless-ui`

