# Competitor feature map (returns-focused, derived-query compatible)
#
# This is intentionally shaped like `docs/05-planning/research/competitor-feature-map.md`:
# - headings MUST look like: "### 2.x ..."
# - features MUST be bullet points under those headings

### 2.4 Returns & Exchanges
- return portal
- RMA workflow
- return reasons taxonomy
- exchanges (swap / replacement)
- store credit issuance
- return label generation (returns-specific)
- warehouse receiving + inspection
- restock / disposition workflow
- refund approvals / policy checks

### 2.5 Automation & Workflows
- return approval workflow
- refund approval workflow
- SLA timers / escalation
- event log / audit trail

