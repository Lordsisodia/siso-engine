# GitHub Search Queries — Admin tables: filters + saved views + bulk actions (v1)
#
# Goal: find OSS primitives for building admin/bulk-edit tooling:
# - table filter state (column filters, global search)
# - saved views (persisted filter/sort/search state)
# - URL sync for filter state
# - bulk actions / selection models
#
# We prefer library-style repos over full dashboards/sites.

## TanStack Table / react-table ecosystem
- `"tanstack table" saved views`
- `"tanstack table" url search params`
- `"tanstack table" column filters hook`
- `"react-table" saved filters`
- `"react-table" column filters`

## Data grid filtering primitives
- `datagrid filter state react hook`
- `data table bulk actions react`
- `table selection model bulk actions react`

## Saved views / filter persistence
- `saved views filter state react`
- `persist table state localstorage react`
- `persisted filters react`

## URL query param sync (admin)
- `admin filters url query params react`
- `search params state admin table react`

## Bulk edit / bulk update patterns
- `bulk edit admin table react`
- `bulk update form table react`

