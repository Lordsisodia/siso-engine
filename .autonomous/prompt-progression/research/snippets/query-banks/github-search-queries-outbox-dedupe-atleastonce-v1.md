# GitHub Search Queries — Outbox pattern + dedupe store + at-least-once consumers (v1)
#
# Goal: OSS patterns/libraries for reliable event & webhook processing:
# - transactional outbox
# - inbox/dedupe tables
# - at-least-once consumers with replay protection
# - dead letter queues
# - idempotent handlers
#
# These are core to Shopify webhook reliability across inventory/returns/support.

## Outbox / inbox pattern
- `transactional outbox pattern open source`
- `outbox pattern postgres`
- `outbox pattern kafka`
- `outbox pattern typescript`
- `inbox pattern idempotent consumer`

## Dedupe / replay protection
- `event deduplication store`
- `deduplication table postgres`
- `replay protection webhook`
- `idempotent consumer pattern`

## At-least-once processing
- `at least once delivery idempotent`
- `exactly once semantics outbox`
- `webhook consumer at least once`

## DLQ + retries
- `dead letter queue open source`
- `retry queue worker idempotent`

