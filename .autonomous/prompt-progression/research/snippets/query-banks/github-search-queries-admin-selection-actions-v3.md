# GitHub Search Queries — Admin selection + actions (v3, avoid generic "bulk")
#
# Goal: libraries/examples that implement selection models + action bars + apply-to-selected UX.
# Avoid generic "bulk insert/update" DB libraries by deprioritizing the word "bulk".

## TanStack Table selection primitives
- `tanstack table selection column`
- `tanstack table indeterminate checkbox`
- `tanstack table row selection example`
- `tanstack table select all across pages`
- `tanstack table getIsAllPageRowsSelected`

## Selection UX
- `indeterminate checkbox table react`
- `shift click selection react table`
- `select all across pages checkbox react`
- `row selection model react`

## Action bars / apply-to-selected
- `selected rows toolbar react`
- `actions bar for selected rows react`
- `apply to selected modal react`
- `multi select actions toolbar react`

## Batching edits (but phrased without “bulk”)
- `batched mutations react query`
- `mutation queue hook react`
- `apply changes to selected items react`

