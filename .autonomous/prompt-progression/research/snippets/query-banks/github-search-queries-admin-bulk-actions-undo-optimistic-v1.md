# GitHub Search Queries — Admin bulk actions + undo/redo + optimistic updates (v1)
#
# Goal: reusable primitives for admin/bulk-edit tooling:
# - row selection models (multi-select, shift-select)
# - bulk actions toolbars
# - batching mutations (apply to N items)
# - optimistic updates + rollback
# - undo/redo stacks for edits
#
# Prefer library-style repos; avoid generic dashboard templates.

## Selection + bulk actions patterns
- `react table row selection bulk actions`
- `bulk actions toolbar react`
- `shift select table react`
- `multi select table react hook`

## Undo/redo primitives
- `react undo redo hook`
- `undo redo state management react`
- `command pattern undo redo javascript`
- `optimistic updates undo redo`

## Optimistic update primitives (React Query / TanStack Query)
- `tanstack query optimistic update helper`
- `react query optimistic update rollback`
- `optimistic update middleware`

## Mutation batching
- `batch update api client react`
- `bulk update mutation react`
- `queue mutations react hook`

## Datagrid focused (common in admin apps)
- `datagrid bulk edit react`
- `datatable bulk update react`

