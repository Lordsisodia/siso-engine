# GitHub Search Queries — Search infra + UI topics (v1)

Goal: find reusable OSS for:
- search backends (Typesense/Meilisearch/etc.)
- adapters + UI components (InstantSearch, autocomplete, facets)
- patterns we can reuse in both storefront + admin.

Notes:
- Topic lists expand to `topic:<token>` queries.
- Run with `--no-derived-queries` to keep this lane focused.
- Use `--min-stars 200` for high-signal, then lower if too sparse.

## Search backends (topics)

- `meilisearch`, `typesense`, `elasticsearch`, `opensearch`, `search`

## Search UI + adapters (topics)

- `instantsearch`, `react-instantsearch`, `algolia`, `autocomplete`, `faceted-search`, `facets`

## Keyword queries (broad but effective)

- `instantsearch react`
- `algolia instantsearch`
- `algolia autocomplete`
- `meilisearch instantsearch`
- `typesense instantsearch`
- `faceted search javascript`
