# Workflow: Edit Ux
# Edit UX

name: edit-ux
command: EU
description: Edit UX
skill: bmad-ux
agent: Sally
complexity: medium
steps:
- name: start
  title: Start
  description: Start Edit Ux
  actions:
  - Initialize workflow
  - Create WIP file
- name: execute
  title: Execute
  description: Execute Edit Ux
  actions:
  - Perform main workflow
  - Track progress
- name: complete
  title: Complete
  description: Complete Edit Ux
  actions:
  - Finalize
  - Update WIP
  - Archive
apc_menu:
  enabled: true
  options:
    advanced:
      label: Advanced
      description: Full workflow with all steps, detailed documentation
    party:
      label: Party
      description: Multi-agent collaborative mode
    continue:
      label: Continue
      description: Resume from last saved checkpoint
wip_tracking:
  enabled: true
  directory: ./wip
  filename_pattern: eu-{name}-{timestamp}.md
outputs:
- type: artifact
  location: ./artifacts/edit-ux-{name}.md
  format: markdown
- type: wip
  location: ./wip/{command}-{name}-{timestamp}.md
  format: markdown
verification:
- WIP file created and updated
- All steps completed
- Output artifacts generated
