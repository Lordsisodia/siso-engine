# Workflow: Sprint Planning
# Sprint Planning

name: sprint-planning
command: SP
description: Sprint Planning
skill: bmad-sm
agent: Bob
complexity: medium
steps:
- name: start
  title: Start
  description: Start Sprint Planning
  actions:
  - Initialize workflow
  - Create WIP file
- name: execute
  title: Execute
  description: Execute Sprint Planning
  actions:
  - Perform main workflow
  - Track progress
- name: complete
  title: Complete
  description: Complete Sprint Planning
  actions:
  - Finalize
  - Update WIP
  - Archive
apc_menu:
  enabled: true
  options:
    advanced:
      label: Advanced
      description: Full workflow with all steps, detailed documentation
    party:
      label: Party
      description: Multi-agent collaborative mode
    continue:
      label: Continue
      description: Resume from last saved checkpoint
wip_tracking:
  enabled: true
  directory: ./wip
  filename_pattern: sp-{name}-{timestamp}.md
outputs:
- type: artifact
  location: ./artifacts/sprint-planning-{name}.md
  format: markdown
- type: wip
  location: ./wip/{command}-{name}-{timestamp}.md
  format: markdown
verification:
- WIP file created and updated
- All steps completed
- Output artifacts generated
